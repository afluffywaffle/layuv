kotlin.code.style=official
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.caching=true

# AndroidX (required by the :app module).
android.useAndroidX=true
android.nonTransitiveRClass=true

# The Android Gradle Plugin does not support JDK 26 (this machine's default).
# Run the Gradle daemon on Android Studio's bundled JDK 21 instead. Override or
# remove this line if your Studio lives elsewhere or you have another JDK 17–21.
org.gradle.java.home=/Applications/Android Studio.app/Contents/jbr/Contents/Home
