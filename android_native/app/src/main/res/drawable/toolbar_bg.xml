<?xml version="1.0" encoding="utf-8"?>
<!-- Floating selection / action toolbar: paper fill, radius 8, 1dp black12
     border, no shadow (Flutter black12 border + BorderRadius.circular(8)). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/paper" />
    <stroke android:width="@dimen/hairline" android:color="@color/ink_12" />
    <corners android:radius="@dimen/radius_btn" />
</shape>
